fetch('cases/cases-v08.json').then(r=>r.json()).then(d=>{const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const links=ids=>(ids||[]).map(id=>`<a href="ledger-v04.html#${encodeURIComponent(id)}">${esc(id)}</a>`).join(' · ')||'—';cases.innerHTML=`<div class="head"><h2>Cases #001–003</h2><p>Dieselbe Untersuchungsmatrix für jedes Unternehmen.</p></div><div class="grid">${d.cases.map(c=>`<article class="box"><span class="pill">${esc(c.status)}</span><h2>${esc(c.case_id)}</h2><b>${esc(c.organisation)}</b><p>${esc(c.question)}</p>${Object.entries(c.lanes).map(([n,l])=>`<div class="lane"><div class="row"><b>${esc(n)}</b><span class="pill ${l.status.includes('open')?'open':''}">${esc(l.status)}</span></div><div>${links(l.claim_ids)}</div><p>${esc(l.gap)}</p></div>`).join('')}</article>`).join('')}</div>`}).catch(()=>cases.innerHTML='<div class="box">Case data unavailable.</div>');